# angular-crud-veiculos

Angular + Bootstrap com módulo de CRUD de veiculos (campos: id, placa, chassi, renavam, modelo, marca e ano).

Consome o backend Node da conversa: `http://localhost:3000/api/veiculos`.

## Rodar
- `npm install`
- Suba o backend Node (porta 3000)
- `npm start`

## Testes
- `npm test`
